import React, { useEffect, useState } from 'react'; import { getProfile, wipeVault } from './db/db'; import { loadCrypto } from './services/crypto'; import Onboarding from './components/Onboarding'; import Dashboard from './components/Dashboard'; import Settings from './components/Settings'; import Game from './games/Game';
export default function App() { const [profile, setProfile] = useState(null); const [game, setGame] = useState(null); const [settings, setSettings] = useState(false); const [loading, setLoading] = useState(true); useEffect(() => { document.documentElement.classList.toggle('comfort-mode', localStorage.getItem('CARE_COMFORT_MODE') === 'true'); Promise.all([getProfile(), loadCrypto()]).then(([p]) => setProfile(p)).finally(() => setLoading(false)) }, []); if (loading) return <div className="min-h-screen flex items-center justify-center"><div className="text-center"><div className="text-5xl">💚</div><p className="font-bold mt-3">Opening your private CARE vault…</p></div></div>; if (!profile) return <Onboarding onComplete={setProfile} />; if (settings) return <Settings profile={profile} onBack={() => setSettings(false)} />; if (game) return <Game gameId={game} profile={profile} onDone={() => setGame(null)} onBack={() => setGame(null)} />; return <Dashboard profile={profile} onGame={setGame} onSettings={() => setSettings(true)} onReset={async () => { if (confirm('Delete this local CARE profile and activity history?')) { await wipeVault(); location.reload() } }} /> }
